<h1>Medication Status in <%= @pharmacy.name %></h1>
<div>
  <% @pharmacy.prescriptions.medications.each do |med| %>
    <%= med.name %>
    <%= med.quantity %>
</div> 
  <% end %>
</div>
WHERE medications.name == med.name on several rows of patients, total count.